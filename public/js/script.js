import icon from '../img/icon-main.js';

// Crea un elemento SVG y asigna el contenido del archivo SVG a él.
const svgContainer = document.getElementById('mainImage');
svgContainer.innerHTML = icon;
